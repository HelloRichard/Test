git commands
ls- list
touch- create a file
mv- rename a file
rm- delete a file
git config --global user.name
git config --global user.email
git config --list
pwd- directory 
cd- change directory 
	~ home
	..going back one 
git add .- add the file
git commit -m "comments"
git log- history
git log --author="name"
git diff- changes made
git commit -am "comments" (changes directly to repository)
git checkout -- file.txt (undo changes)